/*
 * AccountSettings Messages
 *
 * This contains all the text for the AccountSettings component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.AccountSettings.header',
    defaultMessage: 'This is AccountSettings container !',
  },
});
